{
  "name": "tracker",
  "version": "1.0.0",
  "description": "Fullstack tracker",
  "author": "MEINS.. ganz alleine MEINS !",
  "license": "ISC",
  "scripts": {
    "frontend": "cd frontend && ng serve --proxy-config proxy.conf.json",
    "backend": "cd backend && tsc && node dist/index.js",
    "build": "npm i && cd frontend && npm i && cd ../backend && npm i && tsc && cd ../frontend && ./node_modules/.bin/ng build"
  }
}
